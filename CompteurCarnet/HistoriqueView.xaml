<UserControl x:Class="CompteurCarnet.HistoriqueView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{StaticResource BgBrush}" Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Historique"
                   FontSize="24" FontWeight="Bold"
                   Foreground="{StaticResource TextBrush}"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="HistoriqueList"/>
        </ScrollViewer>

        <Button x:Name="BtnRetour" Grid.Row="2" Content="‹ Retour"
                Style="{StaticResource SmallButton}"
                Background="{StaticResource GreyBrush}"
                HorizontalAlignment="Left"
                Margin="0,15,0,0"
                Click="BtnRetour_Click"/>
    </Grid>
</UserControl>
